// Todo lo real vive en src/effects/ (un archivo por efecto + el registro
// central en src/effects/index.ts). Este archivo solo reexporta para que
// el resto del código, que ya importa desde "@/types/effects", siga
// andando sin cambios.
export * from "@/effects";
